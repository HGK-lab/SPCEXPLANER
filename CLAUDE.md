# SPC 설명기 — 작업 규칙

프로젝트 맥락은 `docs/brief.md`. 마감 2026-09-28(월) 17시, 구현은 9/26까지.

## 세션 시작 시
1. `docs/HANDOFF.md`를 먼저 읽고 "다음 할 일"부터 이어서 진행한다.
2. `git status`, `git log --oneline -5`로 HANDOFF 내용과 실제 상태가 맞는지 확인한다. 어긋나면 실제 상태를 기준으로 HANDOFF를 고친다.

## '작업 중지' 명령
사용자가 "작업 중지"라고 하면:
1. 새 작업은 시작하지 않는다. 진행 중인 가장 작은 단위(파일 수정 1개, 테스트 1개)만 마치고 멈춘다. 코드를 깨진 상태로 두지 않는다 — 테스트가 깨졌으면 되돌리거나, 깨진 사실을 HANDOFF에 적는다.
2. `docs/HANDOFF.md`를 갱신한다: 현재 단계, 완료한 것, 진행 중이던 것(파일·함수·어디까지), 다음 할 일(첫 명령까지 구체적으로), 사용자에게 받은 결정, 막힌 점.
3. 변경을 커밋한다: `wip: <요약>` 형식. `.env`와 비밀 값이 스테이징되지 않았는지 `git status`로 확인한 뒤 커밋한다.
4. `git push`로 원격에 올린다 (다른 PC에서도 이어갈 수 있게).
5. 마지막으로 "저장 완료 + 다음 세션 첫 할 일" 한두 줄만 보고한다.

## 코드 규칙
- 모든 코드에 한국어 주석. README에 실행 방법.
- 판정은 규칙 엔진, 설명은 LLM. 역할을 섞지 않는다.
- LLM이 틀린 출력을 내면 삭제하지 말고 `docs/ai_errors.md`에 입력·출력·틀린 점을 기록한다.
- API 키는 `.env`(로컬)와 Streamlit Secrets(배포)에만 둔다. 레포에 커밋 금지.
