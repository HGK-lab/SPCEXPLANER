# 인수인계 (세션 간 이어가기용)

마지막 갱신: 2026-09-26 밤 (구현 Task 1~21 완료, 최종 리뷰·배포 확인 전)

## 현재 단계
구현 계획의 Task 1~21을 모두 마쳤다 (테스트 139개 통과, 매 태스크 커밋·푸시). 다음은 서브에이전트 최종 리뷰(사용자 요청 C) → 심각도 높은 지적 수정 → 배포 확인(D) → 사용자 보고.

## 완료
- GitHub 레포: https://github.com/HGK-lab/SPCEXPLANER (public). 배포: https://spcexplaner-3riy3kwcjzxpuu9ffg5c9q.streamlit.app/ (Streamlit Community Cloud, 브랜치 `main`, 파일 `streamlit_app.py`, main 푸시로 자동 반영). 2026-09-26 저녁 확인: 184ac5d 버전이 정상 표시, 한글 글꼴 로드됨
- 문서: `docs/brief.md`, 설계 `docs/superpowers/specs/2026-09-25-spc-explainer-design.md`(2A 스토리형·기준선 해제·추가 기능 6개 반영), 계획 `docs/superpowers/plans/2026-09-25-spc-explainer.md`(Task 1~21, Task 14-1은 계획 밖 보수 기록), 라이브러리 비교 `docs/research/`, 화면 비교 `docs/design/2026-09-25-ui-comparison.md`, 그래프 시안 `docs/design/ref/`, `README.md`(실행 방법·화면 기능 사용법)
- 구현 (executing-plans 방식, 태스크마다 테스트 먼저 → 커밋·푸시):
  - Task 1 뼈대·설정·패턴 / 2 가상 데이터 생성기 / 3 규칙 판정 + shewhart 교차 검증 / 4 채점 / 5 OpenAI 호출부 / 6 원인표·설명 검증기 / 7 LLM 단독 판정 파서 / 8 리포트 / 9 실험 실행기
  - Task 10 실제 실험 (2026-09-25 23:45~23:48, 호출 168회, 호출 오류 0, 실제 비용 $0.546 = 4.1-mini $0.086 + sol $0.460)
    - 규칙 엔진: 심은 이상 21/21, 오탐 6건(정상 시리즈 1/5 경보)
    - gpt-4.1-mini 단독 판정: 67%(3회 같음), 치우침 1.3/7, 회당 오탐 103건, 정상 시리즈 5/5 경보
    - gpt-6-sol 단독 판정: 100%(3회 같음), 오탐 6건 — 규칙 엔진과 같은 판정, 호출당 평균 10.8초(4.1-mini 2.1초)
    - 설명 LLM(gpt-4.1-mini) 출력 48개 전부 검증 통과 (형식 위반·원인표 밖·판정 불일치 0)
    - 결과 파일: `data/synthetic/series.json`, `results/*.json`, `docs/validation_report.md`, `docs/ai_errors.md`(단독 판정 오답 59건 기록)
  - Task 11 SECOM 실데이터: 선정 센서 88·115, 1567행(`data/secom/secom_selected.csv`). 앞 500점으로 한계 추정, Phase II 알람 사건 sensor_88 24개(불량 포함 4) · sensor_115 10개(불량 포함 1)
  - Task 12·13 스토리형 화면 (머리말·01~05·한계, 04 검증 KPI·탐지율 막대·오류 자동 집계·사례 해설, 05 SECOM), Task 14 README·인수인계
  - Task 14-1 (계획 밖 보수): 최종 리뷰 테스트, 03 반복 결과 줄을 평문(이스케이프한 `st.html`)으로 — LLM id의 마크다운·링크 서식이 해석되지 않게, `CLAUDE.md` 기준선 문구 삭제
  - Task 15 조치 체크리스트(원인표 항목만, 검증 통과한 AI 설명이면 AI 추천 순서) + 인수인계 메모(.md 내려받기·복사) / 16 용어 툴팁(`glossary.py` 한 곳) / 17 관리도 점 클릭 → 사건 강조·펼침·03 이동 링크, 사건 밖 안내, 사건 선택 드롭다운 / 18 첫 방문 30초 가이드 / 19 "오탐이에요" 피드백(세션 안에만, 목록·CSV, 시연용 표기) / 20 06 내 데이터 CSV 올리기·붙여넣기 판정(입력 검사, 앞 N점 추정 또는 직접 한계, 06 사건에도 피드백) / 21 README·이 인수인계
  - 섹션 번호: 06 내 데이터로 판정, 07 한계
  - 화면 확인: 태스크마다 저장소 코드로 앱을 띄워 데스크톱(1440)·모바일(400) 캡처. 점 클릭·피드백·파일 올리기는 데스크톱 브라우저에서 실제로 눌러 확인
- 검증·캡처 도구: `docs/superpowers/tools/` — `extract_plan.py`(계획서 코드만으로 프로젝트 추출), `apply_brief.py`(브리프 코드 적용), `cdp_shot.py`·`cdp_steps.py`(헤드리스 Chrome 캡처·단계 실행, `websocket-client` 필요 — requirements에는 없음), `app_steps.py`(단계 파일 만들기), `run_shots.sh`(앱을 새로 띄워 단계 실행 후 끔)
- 진행 기록(ledger): `.superpowers/sdd/2026-09-25-spc-explainer/progress.md` — git 제외라 이 PC에만 있다. 다른 PC에서는 `git log`로 태스크 완료를 확인한다

## 진행 중
없음.

## 다음 할 일
1. 서브에이전트 최종 리뷰 (사용자 요청 C): 전체 저장소, 기준 = 스펙 준수, 화면 실험 수치가 결과 파일에서만 오는지, LLM 문자열 `esc()` 누락, 업로드 입력 검증, 비밀값 노출, README가 실제 동작과 맞는지. 심각도별로 보고, 높은 것만 고치고 나머지는 목록으로
2. 배포 확인 (D): 배포 화면 데스크톱·모바일 캡처, 한글 글꼴 로딩, 모바일 라벨 겹침, 점 클릭(B3)·업로드(B6) 동작
3. 사용자에게 받을 것: 사례 해설 검토(사용자가 따로 전달 예정), Streamlit Secrets에 `OPENAI_API_KEY`(실시간 설명용, 사용자 작업), 배포 Python 버전을 로컬(3.10)과 맞추기(사용자 예정 — pycontrolcharts 0.1.2는 Python 3.10 이상, 3.10~3.13 지원 표기)
4. 그다음 제출물 (브리프 "최종 결과물"): 데모 영상(2분), 포트폴리오 PDF(8~10쪽), 지원서의 AI 활용 경험 텍스트 — 범위는 사용자와 정한다. 제출 마감 9/28(월) 17시

## 사용자 결정 사항
- LLM: OpenAI 키 사용 (이전 해커톤 키 재사용). 배포: Streamlit Community Cloud. 작업마다 커밋 + 푸시 (상시 허락). 필요하면 OpenAI 프로젝트의 허용 모델 변경 가능
- 2026-09-25 LLM 구성(A안): 설명 = gpt-4.1-mini, 단독 판정 비교 = gpt-4.1-mini vs gpt-6-sol, gpt-6-astra는 설정으로 켜고 끄는 1회 비교(현재 비활성 → 기본 끔). 모델명은 `spc_explainer/config.py` 한 곳에서 관리
- 2026-09-25 가정 전부 승인 + 추가 요구 4가지: (1) 20개 중 정상 시리즈 5개 이상, 오탐 따로 보고 (2) LLM 출력 3회 반복, 형식 위반·원인표 밖 원인·판정 불일치 기록 (3) UCI SECOM 실데이터 확인(결측 적은 센서 1~2개, 앞 구간으로 한계 추정, 탐지율 없이 작동 확인 + 불량 라벨 겹침 관찰) (4) 설계 문서에 "관리한계 고정값 = 안정화된 공정 감시 가정" 명시
- 2026-09-25 그래프: 시안 2장(관리도, 패턴별 탐지율 막대)의 스타일만 따르고 숫자는 결과 파일에서만 읽음. 점 번호 0부터·100점. 정답 구간은 테두리만 있는 점선. LLM 단독 막대는 모델별로 반복 평균 + 최소~최대
- 2026-09-26 화면 2A(한 페이지 스토리형) 채택 — 탭 폐지, 2B(모바일)는 반응형 CSS. 결과 파일 읽기와 SECOM 계산은 캐시, 선택 시 관련 섹션만 다시 그림, 04에 "사례 해설(수동 분석)" 칸(자동 집계와 표기 구분)
- 2026-09-26 Task 2~10 진행 승인, Task 10의 API 약 168회 호출 승인
- 2026-09-26 구현 마감 기준선(9/26 18시) 해제 — 시간 때문에 SECOM이나 화면 항목을 빼지 않는다
- 2026-09-26 저녁 승인: 04 제목 "AI가 판정까지 직접 하면 어떻게 될까" 유지, 사례 해설은 단독 판정 오답 2개로 확정, 실시간 설명 후 페이지 전체 재그리기 유지, 캐시 만료·호출 카운터 잠금은 보류 유지
- 2026-09-26 저녁 추가 기능 6개 승인(B1~B6, 이전 "CSV 업로드 없음" 결정을 뒤집음). 원칙: 판정은 규칙, 설명은 LLM, 둘 다 검증. 화면 실험 수치는 결과 파일에서만, LLM 문자열은 `esc()`
- 설계 중 확인 후 정한 것 (사용자가 바꿀 수 있음): 추세 = 연속 6점(증가 5회, pycontrolcharts `test3_n=5`), temperature는 4.1-mini 0 / sol 기본값(sol이 0을 거부)
- Claude가 정하고 보고한 것 (사용자 확인 전):
  - 06 앞 N점 기본값 = 행 수의 절반과 100 중 작은 값, 최소 20. 추정은 30행 이상(추정 20 + 감시 10)일 때만, 짧으면 직접 입력
  - 섹션 번호 06 내 데이터로 판정, 07 한계 (한계를 맨 끝에)
  - 오탐 피드백 목록은 03 아래 한 곳. 06에서 누르면 앱 전체를 다시 그려 목록을 갱신
  - 관리도 음영 라벨의 줄은 모바일 폭 기준으로 배정 — 데스크톱에서도 줄이 나뉘는 경우가 있음(시리즈 11은 3줄). A3 판단은 최종 보고에
  - 점 클릭의 "03으로 이동"은 자동 스크롤이 아니라 안내 상자의 링크(누르면 03으로 이동)

## 막힌 점
없음. 참고:
- 키로 쓸 수 있는 모델은 gpt-4.1-mini, gpt-6-sol 둘뿐 (astra는 대시보드에서 허용해야 함)
- 헤드리스 모바일 에뮬레이션에서는 탭(버튼·차트 점)이 재현되지 않는다. 휴대폰에서 점 탭 선택 여부는 실제 기기로 봐야 한다 → 사건 선택 드롭다운을 항상 둠
- 2026-09-26 20:03 사용자가 `.devcontainer/devcontainer.json`을 원격에 추가함(Streamlit Cloud 개발 컨테이너, Python 3.11 이미지). 내 커밋을 그 위로 rebase해서 푸시했다

## 계획과 실제 실행의 차이 (포트폴리오용)
계획대로 했는데 실제로는 어긋난 지점과, 그것을 어떻게 잡고 고쳤는지 모은다. 새 사례가 생기면 아래에 덧붙인다.

| # | 날짜 · 단계 | 계획 | 실제로 일어난 일 | 원인 | 조치 |
|---|---|---|---|---|---|
| 1 | 09-26 · 구현 Task 1 | requirements 파일에 한글 주석을 달고 `pip install -r requirements-dev.txt` | 한국어 윈도에서 설치가 `UnicodeDecodeError: 'cp949' codec can't decode`로 멈춤 | Python 3.10에 딸린 pip 23이 인코딩 선언이 없는 파일을 시스템 기본 인코딩(cp949)으로 읽음. 계획 검증 때는 패키지를 직접 설치해서 이 경로를 안 탔음 | 두 파일 첫 줄에 `# -*- coding: utf-8 -*-`을 넣어 한글 주석을 지키고 해결. 계획서 Task 1에도 수정과 이유를 적어 다른 PC에서 재발하지 않게 함 |
| 6 | 09-26 · 구현 Task 5 | 실패 확인 단계의 예상 문구 `ModuleNotFoundError` | 실제 문구는 `ImportError: cannot import name 'llm_client'` | 테스트가 `from spc_explainer import llm_client` 형태라 파이썬이 다른 예외를 냄 (원인은 같음: 모듈 없음) | 실패 원인이 기대와 같은지 확인하려고 구현 파일을 잠시 치워 다시 돌려 본 뒤, 문구 차이만 기록하고 진행 |
| 7 | 09-26 · 구현 Task 10 | 2A 시안의 04 제목 "AI가 판정까지 직접 하면 얼마나 틀릴까" — AI 판정이 틀린다고 전제 | 상위 모델 gpt-6-sol은 3회 모두 규칙 엔진과 같은 판정(100%, 오탐도 같은 6건). 소형 gpt-4.1-mini만 크게 틀림(67%, 회당 오탐 103건) | 결과가 모델 크기에 따라 갈림 | 04 제목을 중립으로 바꾸고, 역할 분리 근거를 정확도에서 결정성·비용·속도·검증 가능성으로 옮김 (사용자 승인) |
| 8 | 09-26 · 구현 Task 10 | "사례 해설"은 ai_errors.md의 설명 LLM 오답에서 고를 계획 | 설명 LLM 출력 48개가 전부 검증 통과 — 설명 오답 0건 | 판정 결과·원인표만 주는 좁은 입력 + JSON 모드 | 단독 판정(gpt-4.1-mini) 오답에서 대표 사례 2개를 골라 해설 (사용자 승인) |
| 9 | 09-26 · 화면 프로토타입 | 음영 라벨을 사건 시작점 위에 한 줄로 | 가까운 사건(#45, #48)의 라벨이 겹침, 모바일에서 더 심함 | 라벨 폭이 점 간격보다 넓음 | 시작점이 25점 이내면 두 줄로 엇갈려 배치 → 재확인에서 10번 사례가 나와 다시 고침 |
| 10 | 09-26 · 계획 재작성 검증 | 9번 조치(가까운 사건만 두 줄로 엇갈림)로 겹침 해결 | 모바일 카드의 실제 그림 폭(약 326px)에서 시리즈 11의 "급변 #45"와 "추세 #76–81" 라벨이 겹침 | 바로 앞 사건과의 거리만 봐서, 좁은 화면에서 길게 뻗는 두 칸 앞 라벨을 놓침 | 좁은 화면(그림 폭 200px) 기준으로 라벨 글자 폭을 어림해 겹치지 않는 첫 줄에 배정, 네 줄 이상이면 라벨 생략 |
| 11 | 09-26 · 구현 Task 12 | 실시간 설명 버튼을 누른 뒤 `st.rerun(scope="fragment")`로 그 섹션만 다시 그림 | 실제 키로 누르는 확인에서 `StreamlitInvalidLayoutContextError` (LLM 호출 1회는 이미 나간 뒤) | Streamlit은 전체 재실행 중에는 fragment 범위 재실행을 막는다. 클릭이 전체 재실행으로 들어오는 경우를 코드가 가정하지 못함 | 앱 범위 `st.rerun()`으로 바꾸고 회귀 테스트 추가. 다시 호출해 3.4초·검증 통과 확인 |
| 12 | 09-26 · 구현 Task 13 | 사례 해설 초안을 그대로 넣음 | 원자료와 숫자를 대조하다가, 시리즈 11에서 gpt-4.1-mini 2회차가 급변 #50(102.50nm, UCL 안쪽)도 보고한 사실이 초안에 빠진 것을 발견 | 초안 작성 때 2회차 출력의 사건 하나를 놓침 | "AI 출력"·"무엇이 틀렸나"에 #50 추가. 나머지 숫자는 모두 원자료와 일치 |
| 13 | 09-26 · 최종 리뷰(1차) | Review Focus 2번: "저장된 설명의 입력이 현재 판정과 다른 경우"를 `test_app_without_saved_files`가 막는다 | 그 테스트는 파일이 없는 경우만 확인하고, 입력이 어긋난 경우는 어느 테스트도 타지 않았음 (코드는 맞게 동작) | 계획 작성 때 두 경우를 한 테스트로 묶어 적음 | `test_stale_saved_explanation_shows_warning` 추가. 경고 코드를 일부러 지운 변형에서 실패하는 것을 확인 |
| 14 | 09-26 · 추가 기능 설계 | 모바일 터치로 관리도 점을 눌러 선택되는지 헤드리스 Chrome으로 확인 | 탭 이벤트 두 방식(`dispatchTouchEvent`, `synthesizeTapGesture`) 모두 차트 점은 물론 일반 버튼도 눌리지 않음 | 헤드리스 에뮬레이션의 한계로 판단(데스크톱 클릭은 모두 정상) | 사건 선택 드롭다운을 항상 두고, 모바일은 배치만 캡처로 확인. 실제 휴대폰 확인은 사용자에게 요청 |
| 15 | 09-26 · 추가 기능 캡처 | 툴팁을 용어 위에 띄움 | 모바일에서 오른쪽 끝 용어(σ)의 툴팁이 화면 밖으로 잘림 | 위치를 용어 왼쪽 끝 기준으로 잡음 | 폭 640px 이하에서는 화면 아래에 고정해 띄움 |
| 16 | 09-26 · 추가 기능 캡처 | 사건 선택 상자의 "선택 안 함" 값을 `None`으로 | 사건 밖 점을 누르면 상자에 영어 "Choose an option"이 뜸 | Streamlit이 `None` 값을 빈 선택으로 봄 | "선택 안 함"을 -1로 |
| 17 | 09-26 · 추가 기능 캡처 | Phase I/II 경계 글자를 그림 위쪽에 | 06 추정 모드에서 경계(#50)에서 시작하는 음영 라벨과 겹침 | 두 글자가 같은 x·같은 높이 | Phase 글자를 그림 아래쪽 안(흰 배경)으로 |
| 18 | 09-26 · 계획 검토 워크플로 | 계획·코드가 추가 기능 요구를 다 채운다고 봄 | 에이전트 30개가 지적 24건 → 반박 검증 뒤 13건 확인: 빈 파일 업로드가 오류 대신 대기 안내, 한계 밖 점이 길게 이어지면 원시값 수천 개가 LLM 입력·라벨에 들어감, csv 오류·NUL로 예외, 1행 결측·NaN을 머리글로 오인, 아주 큰 값에서 예외, 작은 단위 데이터에 y축 눈금 없음, 06 호출 뒤 03 남은 횟수가 늦게 갱신, 가이드의 시리즈 수 고정, 06 사건에 피드백 없음(요구를 좁힘), 내려받기 버튼을 개수로만 확인, AI 순서 검증 문(gate) 테스트 없음, 새 도구 미추적, README 문구 3곳 | 한 사람(작성자)이 쓰고 확인한 계획의 사각지대 | 13건 모두 테스트 먼저 넣고 고침. 기각 11건은 근거를 남김 |
| 19 | 09-26 · 작업 중단 | 계획 검토 워크플로를 기다린 뒤 이어서 진행 | 세션 제한으로 작업이 멈춤. 워크플로는 백그라운드에서 끝까지 돌았고 결과는 journal에 남음 | 세션 사용량 한도 | 다음 세션에서 journal을 읽어 결과를 복구하고 그대로 이어감 |

### 보류한 작은 개선 (사용자가 원하면 진행)
- 결과 파일 캐시(`st.cache_data`)에 만료가 없어, 앱을 띄운 채 실험을 다시 돌리면 앱을 다시 시작해야 새 숫자가 보인다 (배포는 푸시마다 재시작) — 보류 유지(사용자 결정)
- 하루 실시간 호출 수 카운터는 세션 사이에서 잠금 없이 더한다 — 보류 유지(사용자 결정)
- 관리도 라벨 폭은 IBM Plex Sans KR 기준 어림값이라, 글꼴을 못 받아 다른 글꼴로 그려지면 좁은 화면에서 겹칠 수 있다

계획을 쓰는 동안 실제로 돌려 보고 잡은 것 (구현 전):

| # | 날짜 · 단계 | 계획(처음 생각) | 실제로 일어난 일 | 원인 | 조치 |
|---|---|---|---|---|---|
| 2 | 09-25 · 설계 | 브리프의 "추세 = 6점 연속 상승"을 pycontrolcharts 기본값으로 판정 | 기본값은 7점(증가 6회)을 요구, 비교용 shewhart는 6점에서 탐지 — 두 라이브러리 판정이 다름 | `test3_n`이 점 개수가 아니라 "증가 횟수" | `test3_n=5`로 브리프 정의에 맞추고, 두 라이브러리의 점 단위 일치를 테스트로 고정 |
| 3 | 09-25 · 설계 | 모든 모델을 temperature 0으로 호출 | gpt-6-sol이 거부: "Only the default (1) value is supported" | 모델 제약 | temperature를 모델별 설정으로 두고 sol은 기본값 |
| 4 | 09-25 · 계획 검증 | 막대 라벨과 AI 출력 발췌를 모노 글꼴(IBM Plex Mono)로 표시 | 실제 화면에서 한글 자간이 벌어져 보임 | 모노 글꼴에 한글 글리프가 없어 대체 글꼴로 그려짐 | 한글이 들어가는 두 곳만 산세리프로 |
| 5 | 09-25 · 계획 검증 | 마커 트레이스 이름에 기호 포함("◆ 급변") | SECOM 탭 범례가 "◆ ◆ 급변"으로 겹침 | Plotly 범례가 마커 모양을 이미 그림 | 트레이스 이름에서 기호 제거 |
